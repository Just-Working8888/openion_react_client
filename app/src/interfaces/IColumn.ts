export interface IColumn {
  title: string;
  dataIndex: string;
  key: string;
  width?: number | string;
  // isActive?: boolean;
  // render?: any;
  // sorter?: any;
  // onFilter?: any;
}
